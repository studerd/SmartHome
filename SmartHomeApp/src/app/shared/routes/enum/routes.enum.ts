import {AppNode} from './node.enum';

export enum AppRoutes {
  PUBLIC = AppNode.DEFAULT,
  ABOUT_US = AppNode.ABOUT_US,
  JOBS=AppNode.JOBS,
  OUR_VALUE=AppNode.OUR_VALUE,
  JOIN_US=AppNode.JOIN_US
}
